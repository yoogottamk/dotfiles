#!/bin/bash

path_dirs=(
    "$HOME/bin"
    "$HOME/.gem/ruby/2.7.0/bin"
    "$HOME/.apps/whatsapp-linux-linux-x64"
    "$HOME/.local/bin"
    "$HOME/proj/spotify-control"
    "$HOME/proj/todo"
    "/usr/local/android-studio/bin"
    "$HOME/.cargo/bin"
)

appended_path=$(IFS=":$IFS"; printf "%s" "${path_dirs[*]}")

export PATH="$PATH:$appended_path"
